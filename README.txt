Navigate to project folder:
$ mvn package 		creates executable jar file

Run app from StartApplication class in IDE:
localhost:8080 				displays "Hello World!"
localhost:8080/example		displays "Hello World!"

Run app from jar file location:
$ java -jar target/mySpringBootApp-0.0.1-SNAPSHOT.jar